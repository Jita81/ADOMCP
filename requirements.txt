# ADOMCP - MCP SDK Compliant Server Requirements
# Official MCP Python SDK and dependencies

# Core MCP SDK
mcp==1.14.1

# HTTP Client for API calls
aiohttp>=3.12.0

# Additional dependencies included with MCP SDK:
# - anyio>=4.5 (async I/O)
# - httpx>=0.27.1 (HTTP client)
# - httpx-sse>=0.4 (Server-Sent Events)
# - jsonschema>=4.20.0 (JSON schema validation)
# - pydantic>=2.11.0 (data validation)
# - pydantic-settings>=2.5.2 (settings management)
# - python-multipart>=0.0.9 (multipart form support)
# - sse-starlette>=1.6.1 (SSE for Starlette)
# - starlette>=0.27 (ASGI framework)
# - uvicorn>=0.31.1 (ASGI server)

# CLI support
click>=8.0.0
